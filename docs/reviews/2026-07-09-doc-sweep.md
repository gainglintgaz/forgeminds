# ForgeMinds — Documentation Hygiene Sweep (2026-07-09)

> **Scope:** founder request — "review all md, skills, agents, goals to see if they need to be updated, revised, improved, adjusted, added, deleted, compacted."
> **Method:** every claim below was checked against git log + live files (lesson #108 — docs vs live truth), not against prior handoffs.
> **Two buckets:** APPLIED (pre-approved by `v1-execution-plan-2026-07-08.md` §"Doc updates" + routine VIBE-Rule-40 tracking) vs PROPOSE-ONLY (below — nothing edited; founder approves each).

---

## A — Applied this sweep (for the record)

| File | Change | Authority |
|---|---|---|
| `.claude/CLAUDE.md` | Stack line: host = Vercel (DECISIONS 2026-07-03); Anthropic core loop (Haiku score / Sonnet generate); Gemini retired (`b8ff95d`); one-liner's "brief via Claude Haiku" → Sonnet (verified vs `src/lib/ai/router.ts`) | Execution plan §"Doc updates" (a) |
| `DECISIONS.md` | **Skipped — already done** in `41e52db` (2026-07-03 Vercel-supersedes-Railway entry present at line 761) | Execution plan §"Doc updates" (b) |
| `errors-fixed.json` | ERR-028 → FIXED-in-code (654113d layer-1; per-row isolation deliberately not built; live proof at E6). ERR-029 annotated: E1 relevance gate passed LOCALLY 2026-07-09 (`1123f8b`/`a8f31d2`), stays OPEN until E6 live-cron proof | Execution plan §"Doc updates" (c) |
| `docs/architecture/v1-execution-plan-2026-07-08.md` | E1 four 🤖 boxes checked (`1123f8b`), E2 three 🤖 boxes checked (`a8f31d2`, weave = `c464a43` re-verified); every 🧑 founder box left UNCHECKED; doc-updates bullets annotated done | Routine tracking (VIBE Rule 40) |
| `CURRENT_SPRINT.md` | New top section "2026-07-09 E-SLICE STATUS" superseding the S1–S7 list: E1/E2 proven locally, H1/I1 pending approval, founder next-actions, Cloudflare-contamination parked note | Routine tracking |
| `NEXT_SESSION.md` | New superseding 2026-07-09 header (07-03 block kept as history): the 07-03 "AI pipeline is NOT firing" headline is now stale — E1/E2 prove the pipeline fires locally with real Claude calls | Routine tracking |

---

## B — Propose-only findings (nothing edited; ranked by importance)

### B1. `GOAL.md` — the bar contradicts the approved V1 (HIGH — founder decision required)

- **What's wrong:** GOAL.md (locked 2026-05-16, last touched then) defines V1-ready as "≥5 external alpha users × 4 weeks × Voice-DNA delta ≥+10pp × Community-Brain k=5 hit" with a ≥70/100 composite. The founder-APPROVED finance-core V1 (2026-06-14) + the 2026-07-08 execution plan define the V1 verdict as the **E6 dogfood: beat Pipedream by +0.5 over 5–7 trading days for ONE user (the founder)**. Both cannot be "the bar." GOAL.md's own header says it may only change via PR + DECISIONS entry — so this is flagged, not fixed. Lesson #108 class: the highest-altitude doc in the repo currently disagrees with the approved plan.
- **Proposed action:** founder-approved GOAL.md amendment: keep the 6-axis scorecard, but re-state §3.1 as two stages — Stage 1 (V1 verdict) = the E6 dogfood gate; Stage 2 (paid-ship bar) = the existing external-alpha criteria. Add the runtime-truth gate (`ai_calls_made>0` + human-rated real output) that PENDING_APPROVALS has proposed since 06-14. One DECISIONS entry.
- **Effort:** ~30 min once founder decides.

### B2. `.claude/CRITICAL.md` — the always-loaded digest feeds every session stale facts (HIGH)

- **What's wrong:** §0 says scoring = "Gemini Flash" and brief generation = "Claude Haiku" — both wrong since `b8ff95d` (Anthropic-only: Haiku scores, Sonnet generates). "Last updated 2026-05-18." Because this is the FIRST file every session loads, each new session starts with a wrong mental model of the AI loop (the exact drift class ERR-019/029 punished). §7's MCP table is fine (Vercel-hosted is correct again after the Railway detour). §1's "Rule 35 mechanical pre-commit enforcement" claim should be softened to match the factory's corrected "aspirational unless .githooks installed" language.
- **Proposed action:** update §0 stack sentence + generation models; sync §1 Rule-35 enforcement wording; bump "Last updated." Small diff, but it's the rule digest — founder sign-off appropriate.
- **Effort:** 15 min.

### B3. `V1_FEATURE_BACKLOG.md` — 2000+ hours stale; a tracking file that lies (HIGH)

- **What's wrong:** last touched 2026-04-30. Every row is `DISCUSSED` (one `IN PROGRESS`), yet in reality most Phase 0–2 rows are long built (scaffold, schema, auth, fetchers, cron, scoring, curation, briefs UI...) and the roadmap itself was superseded twice (Phase-2 alpha → S-slices → E-slices). VIBE Rule 40 makes tracking files load-bearing; this one actively misleads any session that reads it.
- **Proposed action:** replace the body with a short pointer: "Superseded — live path is `docs/architecture/v1-execution-plan-2026-07-08.md`; feature parking lot is `IDEAS_BACKLOG.md`" + move the table to an `## Archive (2026-04-30 snapshot)` section, or delete the table outright. Do NOT try to re-status 40 rows by hand — that recreates the drift.
- **Effort:** 15 min.

### B4. `PENDING_APPROVALS.md` — 9 open items; several stale, one now WRONG (MEDIUM-HIGH)

Founder triage pass needed. Item-by-item:

| Item (date) | Verdict | Why |
|---|---|---|
| Email E2 launch checklist (06-11) | **Keep open** | Legitimately blocked on domain purchase; still accurate |
| Self-Reflection proposal #1 — runtime-truth DoD gate (06-14) | **Approve-worthy, still open** | The exact gate ERR-029 later proved necessary; not yet in vibe-standard/execution |
| #2 — Rule 24 strict-resolution-to-UUID (06-14) | **Approve-worthy, still open** | Built in code (GP-015) but never promoted to the rule text |
| #3 — GOAL.md dogfood gate (06-14) | **Merge into B1 above** | Same decision |
| #4 — Stack section → **Railway** (06-14) | **REJECT / mark superseded** | Now factually wrong — Vercel superseded Railway (DECISIONS 2026-07-03); the Stack line was fixed this sweep |
| #5 — promote lessons #104–110 to factory archive (06-14) | **Still open** | Factory `lessons-critical.md` carries #103 max; #104–111 live only in the project overlay |
| SIGNAL-AUTO REWORK 3x (06-10) | **Resolvable** | The drawing-board reset + honest audit WAS the architectural review it asked for |
| SIGNAL-AUTO APPROVAL 3x (06-10) | **Resolvable** | Golden-path capture happened (GP-014/015); close |
| SIGNAL-AUTO SECURITY 1x (06-14) + REPEAT 1x (06-27) + SECURITY-LOW (07-02) | **Review + close** | 07-02 entry self-diagnoses as false positive; the 06-14 one needs a 2-min signal-log check, then close |

- **Effort:** one 20-min founder pass; the file then shrinks ~60%.

### B5. `golden-paths.md` — two proven E1 patterns missing (MEDIUM)

- **What's wrong:** only GP-014/GP-015. Two patterns from the E1 session earned entry:
  - **GP-016 (proposed) — defensive LLM-JSON parse: strip markdown code fences before `JSON.parse`.** Every score batch was silently failing on ```json fences; no article had ever received a real personalized relevance score locally until `1123f8b` fixed it. Pair with the ERR-027/029 golden rule: never swallow the parse error into defaults.
  - **GP-017 (proposed) — local pipeline runs with secrets via `--env-file` / env-scoped invocation, never Read/echo of `.env*`.** The E1/E2 local proofs ran the full pipeline with real keys without any secret entering a transcript (secrets-handling §2 compliant). Pin the exact invocation snippet from the E1 session when writing the entry.
- **Proposed action:** append both entries (append-only file; founder approves per Phase 6 learning-loop rules).
- **Effort:** 15 min.

### B6. Project `.claude/rules/mcp-tools.md` — wrong project's facts (MEDIUM)

- **What's wrong:** contains `FinKeel project_id: trcrzkeeceocfsrmfxfm` (this is ForgeMinds — dev is `ymgbjtgczgnooscigplb`); pins `grok-3-latest` (grok-3 family retired 2026-05-15 per DECISIONS — the registry now owns the pin); "Tech Defaults" say Vite + React (ForgeMinds is Next.js 16). `CRITICAL.md` §7 already carries the correct compact MCP table.
- **Proposed action:** either delete the file (CRITICAL.md §7 + factory `mcp-servers.md` cover it) or strip it to ForgeMinds-true facts. Deletion is cleaner — one less mirror to drift.
- **Effort:** 10 min.

### B7. Project `.claude/rules/execution.md` overlay — masks newer factory doctrine (MEDIUM)

- **What's wrong:** the project overlay still has the OLD soft "Phase 0: Probing Questions" and no mention of the Senior Council / Bridge Brief / discovery-protocol that the factory `execution.md` promoted on 2026-05-17. Per the hierarchy (project rules outrank factory), the stale overlay actually SHADOWS the stronger factory process. It also hardcodes "Victor" (factory files were de-personalized) and cites `.husky/pre-commit` (project uses no husky).
- **Proposed action:** either sync the overlay to the factory version + keep only ForgeMinds-specific additions (the Communication Rule, Phase 5.5 AUDIT GATE block — both worth keeping), or delete the overlay and rely on the factory file. Recommend sync-and-trim.
- **Effort:** 30 min.

### B8. `IDEAS_BACKLOG.md` — tier gates reference a paused plan (LOW-MEDIUM)

- **What's wrong:** T0/T1 gates are phrased against the Phase-2 closed alpha ("post 5-user × 4-week alpha") which is explicitly paused behind the E-slices; T0.2 (brief loop) is effectively delivered; T0.4 (backup) is now the E6 P7 item. The tiers are still directionally right — this is drift, not contradiction.
- **Proposed action:** add a short 2026-07-09 header note: "Tier gates now key off the E-plan: T1 unlocks after the E6 dogfood PASS (not the 5-user alpha, which moved post-V1)"; mark T0.2 delivered-via-E1/E2 and cross-reference T0.4 → E6 P7. No restructure.
- **Effort:** 15 min.

### B9. `.claude/agents/*` — usable but pre-reset vintage (LOW)

- `source-catalog-curator.md` + `source-validator.md` (2026-05-05): still architecturally valid (catalog table + strict URL validation match VIBE Rule 56), but both predate the finance-first reset — neither mentions the finance-source-quality lever (the S3 "honest limit": world/macro sources rarely name tickers) which is the exact job the curator would be used for next. Proposed: one paragraph each pointing at the finance-first scope + the `sources` seeding pattern from `1123f8b`. Keep both.
- `phase-auditor.md` (2026-05-05): checklist mechanics still good; references "phases" while the live plan uses E-slices, and predates the runtime-truth lesson (#104/#111 — it must check `pipeline_runs.ai_calls_made>0`, not just static gates). Proposed: add the runtime-truth query to its check list; rename references phase→slice. Keep.
- `senior-architect` (factory-level agent, no project override): its REQ.md→ARC.md workflow doesn't match this project's ARCHITECTURE.md flow (`docs/architecture/*.md` + architect-probe). Not broken — just note that H1/I1-style probes are the project's actual pattern; no project agent file needed.
- **Effort:** 30 min total.

### B10. `.claude/checklists/` — historical records, one live template (LOW)

- `phase-0/1/1.5-*` files are completed audit records — keep as archive, no action. `phase-audit-template.md` is the live input to phase-auditor; same runtime-truth addition as B9 applies. `phase-template.md` fine.
- **Effort:** covered by B9.

### B11. Rule-header cosmetic contradiction (LOW)

- Project (and factory) `vibe-standard.md` header says "v5.3 — 54 Mandatory Rules" while the file defines 60 (Rules 55–60 added later). Cosmetic, but it is the kind of internal contradiction that erodes trust in the corpus. Propose header bump to "60 rules" at the next factory rules release (factory-owned; project mirror follows).
- **Effort:** 2 min (factory-side).

---

## C — Lesson #108 flags raised this sweep (docs that disagreed with live truth)

1. `NEXT_SESSION.md` 07-03 headline ("AI pipeline is NOT firing") — true then, stale now; superseded block added (bucket 1).
2. `GOAL.md` §3.1 vs the approved E6 dogfood gate — B1, open.
3. `V1_FEATURE_BACKLOG.md` all-DISCUSSED vs a built pipeline — B3, open.
4. `PENDING_APPROVALS` proposal #4 (Stack→Railway) vs DECISIONS 2026-07-03 (Vercel) — B4, recommend reject.
5. `.claude/CRITICAL.md` §0 (Gemini Flash / Haiku-generates) vs `src/lib/ai/router.ts` (Haiku scores / Sonnet generates) — B2, open.
6. Project `mcp-tools.md` FinKeel project-id inside the ForgeMinds rule tree — B6, open.

---

*Nothing in section B was edited. Each item awaits founder approve/reject; B1 and B4 need founder judgment, the rest are mechanical once approved.*
