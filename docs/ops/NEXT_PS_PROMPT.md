# PS Claude task — reconcile the stalled-loop landmines (code-only; NO deploy, NO DB writes)

> Hand this to a PowerShell Claude Code session running in
> `C:\Users\vtbsj\victor-ai-factory\projects\forgeminds`.
> Generated by the diagnosis session, 2026-06-04. Do NOT commit this file.

## CONTEXT (you have no memory of the diagnosis — here it is)

The automated pipeline has been dead since 2026-05-17. Confirmed root cause via DB:
pg_cron fires every minute and the dispatcher correctly selects the due user, but every
HTTP POST dies with "Couldn't resolve host name" — because `private.app_config
.forgeminds_base_url = 'https://forgeminds.app'` (a placeholder domain that doesn't resolve)
AND ForgeMinds is not deployed anywhere. The real fix (deploy + repoint base_url) is being
handled separately/interactively. **DO NOT deploy, DO NOT touch `private.app_config`, DO NOT
write to any database.** Your job is code-side cleanup only.

**LANDMINE found (lessons-critical #38, migrations-are-stale):**
- File `supabase/migrations/20260501000001_pg_cron_dispatcher.sql` defines
  `private.invoke_forgeminds_cron` to read base_url from a GUC
  (`current_setting('app.forgeminds_base_url')`) and ENDS by pausing all 6 cron jobs.
- The LIVE function instead reads base_url from the table `private.app_config`, and the 6
  jobs are `active=true`. The GUC is set nowhere. Repo ≠ reality; a fresh `supabase db reset`
  would regenerate broken functions.

## TASKS (code/docs only; master branch; run the 5 verify gates)

### 1. RECONCILE THE MIGRATION to match the live DB
- First pull GROUND TRUTH (don't trust this summary) via the `supabase-forgeminds` MCP:
  ```sql
  SELECT pg_get_functiondef(p.oid) FROM pg_proc p JOIN pg_namespace n
    ON n.oid=p.pronamespace WHERE n.nspname='private'
    AND p.proname IN ('invoke_forgeminds_cron','dispatch_forgeminds_cron');
  SELECT to_regclass('private.app_config');  -- plus its columns
  ```
- Add a NEW forward migration `supabase/migrations/<ts>_reconcile_cron_app_config.sql`:
  create `private.app_config (key text PK, value text)` IF NOT EXISTS, seed the
  `forgeminds_base_url` row with `ON CONFLICT DO NOTHING` (keep value `https://forgeminds.app`
  — the real URL is set separately, NOT by you), and `CREATE OR REPLACE` both functions to
  EXACTLY match the live definitions you pulled. Include a commented DOWN section and a header
  explaining it reconciles the stale `20260501000001` migration to deployed reality.
  DO NOT edit the old migration (preserve history). DO NOT apply it (DB already matches).
- Commit: `fix(cron): reconcile pg_cron dispatcher migration to live DB [discovery-skipped] reason: refactor to match deployed reality, no behavior change`

### 2. FIX THE COST-PRICING DRIFT
- In `src/lib/ai/` (`models.ts` / `router.ts`): the model was bumped to `gemini-2.5-flash`
  but `COSTS.GEMINI_*` per-token constants still hold `gemini-2.0-flash` pricing, so cost
  logging is wrong for every score/categorize call. Look up current 2.5-flash pricing, update
  the constants with a sourced comment, remove now-dead 2.0 entries.
- Commit: `fix(ai): correct gemini cost constants to 2.5-flash pricing`

### 3. DEPLOY-READINESS AUDIT (report only; NO deploy)
- Audit all 6 cron routes (`src/app/api/cron/{ingest,score,curate,generate,deliver,enrich}/route.ts`):
  each must (a) reject `Authorization != Bearer ${CRON_SECRET}`, (b) resolve `?user_id`.
  Write findings to `docs/ops/deploy-readiness.md` (table: route | auth Y/N | user_id Y/N | notes).
- Ensure `.env.example` lists ALL server env vars (placeholders only): `NEXT_PUBLIC_SUPABASE_URL`,
  `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `GEMINI_API_KEY`,
  `XAI_API_KEY`, `FINNHUB_API_KEY`, `ALPACA_API_KEY`, `ALPACA_SECRET_KEY`, `ALPHA_VANTAGE_KEY`,
  `BENZINGA_API_KEY`. Add a note: CRON_SECRET on the host MUST equal the Supabase vault
  `cron_secret` or every cron route returns 401.
- Commit: `chore(ops): deploy-readiness audit + complete .env.example`

## CONSTRAINTS
- Stay on master (project rule: no worktrees). Scope each commit with `git add <specific paths>`
  — there is an uncommitted edit to `docs/decisions/2026-05-31-ai-model-ab.md` from another
  session; do NOT include it in your commits, and do NOT commit this prompt file.
- No DB writes, no migration apply, no deploy, no prod. Dev project = `ymgbjtgczgnooscigplb`.
- Do NOT build DSR / any Phase 2.3 feature — the loop (Phase 2.1) must run first.
- Five gates before any done-wording: `npx tsc --noEmit`, `npm run lint` (browser/DB n/a here),
  column-drift grep if you touched a query. AUDIT GATE block in any done/complete commit body.
- Report back: the 3 commit hashes + the deploy-readiness table.
