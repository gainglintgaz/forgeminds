# Session Handoff — 2026-05-06

> **Read this first.** It carries the context from a long session that closed Phase 1.5 schema work and prepped Phase 2 + Phase 1.5 catalog-seeding. Future sessions read this so I don't re-derive what's already done.
> **This file is committed to git** — delete after Phase 1.5 closes if it gets stale. Update as state changes.

---

## TL;DR — where we are

| Phase | State | What's truly left |
|---|---|---|
| **Phase 0** | ✅ COMPLETE (commit `d09300a`) | nothing |
| **Phase 1** | ⏳ Code complete; 7 manual close blockers ~15 min total | Bootstrap SQL paste, run `verify:phase-1` with dev server, two-user RLS smoke, hook test, phase-auditor re-run, close commit |
| **Phase 1.5** | ⏳ ~70% — schema + skeleton + 4 polish + embed script all landed; **catalog SQL is APPLIED to dev project**; remaining: catalog seeding via curator subagent (~3-5 sessions) + smoke test + close commit |
| **Phase 2 prep** | ✅ DRAFTED (`dbafdbf`) — `article_outcomes` migration + UI ready to apply at Phase 2 start, file-only |

**Bottom line: zero open code work. Next session is curator subagent dispatch + Phase 1.0 manual close, in either order.**

---

## What got done in the prior session (2026-05-05 / 06)

### Code committed (12 commits)

```
5903c57 chore(phase-1-5): record P1.5-A/B/C applied + advisor delta
42c29ed docs: VECTORS.md (14 + ~140 subcategories) + Architect+ tier decision
f278ece wip(phase-1-5): embed-source-catalog backfill script
fcf6828 wip(phase-1-5): polish 3/3 — locked-state on /intake when catalog thin
af01945 wip(phase-1-5): polish 2/3 — structured cost-audit log on chat route
3699476 wip(phase-1-5): polish 1/3 — substring-validate agent's reason field
dbafdbf wip(phase-2): article_outcomes migration + brief-view save/dismiss/rate UI
df1458d docs(rules): adopt ai-first-principles + data-flywheel for ForgeMinds
23beb92 chore(phase-auditor): extend with Phase-1.5-specific checks
4396461 docs(phase-1-5): CURRENT_SPRINT, DECISIONS, IDEAS, ARCHITECTURE_NOTES
938ceb6 feat(phase-1-5): /sources page redesign — advisor + catalog + health
8c64d70 feat(phase-1-5): verify orchestrator + checklist + catalog gate
be44173 feat(phase-1-5): source-validator runtime + onboarding e2e stubs
818b465 feat(phase-1-5): onboarding wizard skeleton (intake → refine → confirm)
ab150bf chore(verify-columns): allowlist pending-migration tables
e2f8991 feat(phase-1-5): add claude/openai/perplexity providers + wire router
3817625 wip(phase-1-5): scaffold source_catalog seeds dir + README
1fa7833 wip(phase-1-5): fix source-catalog-curator example SQL
09a2bd2 wip(phase-1-5): add source_catalog + source_suggestions schema
```

Every commit passed pre-commit gates (tsc + eslint + verify:columns + secret grep).

### SQL applied to dev Supabase project (`ymgbjtgczgnooscigplb`)

Via Supabase SQL editor (manual paste, MCP routing fight blocked automation):

1. `supabase/migrations/20260510000000_source_catalog.sql` — table + enums + indexes + RLS + grants
2. `supabase/migrations/20260510000001_source_suggestions.sql` — table + enums + indexes + RLS + trigger + grants
3. `supabase/seeds/source_catalog_rag_rpc.sql` — `match_source_catalog` RPC; **switched to SECURITY INVOKER** post-apply to eliminate advisor warning. Committed file already reflects INVOKER state.

### Verification (verbatim from SQL editor)

- `select table_name from information_schema.tables where table_schema='public' and table_name in ('source_catalog','source_suggestions');` → 2 rows ✅
- `select proname, proconfig from pg_proc where ... proname='match_source_catalog';` → 1 row, `proconfig = {search_path=public, pg_temp}` ✅
- `select count(*) from public.source_catalog;` → 0 (catalog empty, ready for seeding) ✅

### Advisor scan post-apply: **5 warnings, all known/accepted, ZERO new from Phase 1.5**

| # | Warning | Status |
|---|---|---|
| 1-3 | `extension_in_public` for pg_trgm/vector/pg_net | Deferred to Phase 2 cleanup migration (DECISIONS.md 2026-05-04) |
| 4 | `track_event` SECURITY DEFINER callable by authenticated | INTENTIONAL by design (DECISIONS.md 2026-05-04) |
| 5 | `auth_leaked_password_protection` | Dashboard toggle requiring Supabase Pro tier; deferred to Phase 10 launch prerequisite (IDEAS.md) |

### Strategic artifacts captured (so future sessions don't re-derive)

- `AI_FIRST_AUDIT.md` — ForgeMinds-specific 5-question audit (all 5 pass), Trust Ladder mapping, ~20 features × current gating state, anti-fabrication enforcement matrix
- `DATA_FLYWHEEL.md` — domain mapping (article = entity, brief delivery = cycle), 7 contribution types mapped to ForgeMinds tables, k=N choices, cold-start copy, sequenced phases A-G mapped to roadmap, 8 LOCKED-state copy templates, schema migration plan
- `VECTORS.md` — 14 vectors (investment/build/content/network/learn/consulting/land_grab/local_civic/family/travel/health/career/sports_fantasy/legal_tax) + ~140 subcategories + Architect+ tier model with BYOS/Resold/Off cost-recovery (no pre-pay before user commitment)
- `.claude/rules/ai-first-principles.md` + `.claude/rules/data-flywheel.md` — universal rules adopted from easeaway-test
- Updated factory rules: VIBE Rule 57 + Factory CLAUDE.md §4 #19 + lessons #99-100 (AI-at-the-Core principle, factory-level — apply to every project)

---

## What's left (concrete, next session)

### Phase 1.0 close — 7 manual blockers (~15-20 min total)

| # | Action | Time |
|---|---|---|
| **P1.0-A** | `npm run print:bootstrap-sql \| clip` → paste in Supabase SQL editor → Run. Installs vault `cron_secret` + `app_config.forgeminds_base_url` + activates 6 dispatcher cron jobs + applies tool_capabilities seed | 30 sec |
| **P1.0-B** | Leaked Password Protection toggle — DEFERRED (requires Supabase Pro, currently Free) | n/a |
| **P1.0-C** | Terminal 1: `npm run dev`. Terminal 2: `npm run verify:phase-1`. All gates green incl. Playwright e2e | 5 min |
| **P1.0-D** | Two-user RLS smoke: signup as second user, query first user's briefs as user B → expect 0 rows | 3 min |
| **P1.0-E** | Pre-commit hook violation test: try commit with subject "feat: dashboard complete" without AUDIT GATE block, confirm hook rejects | 2 min |
| **P1.0-F** | Re-run phase-auditor subagent for phase-1, confirm 0 open blockers | 5 min |
| **P1.0-G** | `feat: phase 1 complete` commit with AUDIT GATE [phase-1] block + PHASE AUDIT block in body | 2 min |

### Phase 1.5 final — catalog seeding + 5 close steps

API keys ALREADY in `.env.local`: `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `PERPLEXITY_API_KEY`, `XAI_API_KEY`, `GOOGLE_API_KEY`. (Confirmed by user 2026-05-06.) **Don't re-prompt for these.**

| # | Action | Owner | Time |
|---|---|---|---|
| **P1.5-E** | Dispatch `source-catalog-curator` subagent for ≥10 (category, subcategory) pairs to seed ≥200 verified sources. Recommended order: medicine/oncology, finance/monetary_policy, tech/ai_ml, sciences/climate, geopolitics/global_affairs, education/edtech, arts/literature, sports/strategy, lifestyle/longevity, civic/local_govt | Claude (subagent) + Victor reviews | ~3-5 sessions |
| **P1.5-F-runs** | After EACH curator seed batch lands (paste SQL into editor): `npm run embed:catalog` to backfill embeddings | Victor | 30 sec/batch |
| **P1.5-H** | Real onboarding round-trip smoke: `npm run dev`, sign in, navigate `/onboarding/intake`, type real description, watch /refine render proposals, finalize → `/dashboard?from=onboarding` → verify rows in `sources` table | Victor | 10 min |
| **P1.5-I** | Cost-cap audit: ≥3 onboarding runs, grep Vercel logs for `"event":"onboarding_chat"`, confirm mean cost <$0.06, max <$0.10. Already wired via structured log (`af01945`) | Victor | 5 min |
| **P1.5-J** | `npm run verify:phase-1-5` with dev server up — every gate green incl `verify:source-catalog` (≥200 rows, ≥10 categories, median quality ≥0.65, ≥95% with embeddings) | Victor | 5 min |
| **P1.5-K** | Re-run phase-auditor for phase-1-5, 0 open blockers | Victor | 5 min |
| **P1.5-L** | `feat: phase 1.5 complete` commit with AUDIT GATE [phase-1-5] + PHASE AUDIT blocks | Victor | 2 min |

### Phase 2 prep — already drafted, applies at Phase 2 start

`supabase/migrations/20260601000000_article_outcomes.sql` is committed (`dbafdbf`) but NOT applied to dev. Apply when Phase 2 starts. Brief-view save/dismiss/rate UI lands at the same time.

`scripts/verify-columns.ts` `PENDING_MIGRATION_TABLES` still has `article_outcomes` in it — remove when applied.

---

## Known issues / gotchas (read before starting)

1. **Supabase MCP routing is messy.** Two MCPs share `mcp.supabase.com/mcp` (the user-level plugin + this project's `supabase-forgeminds`). Claude Code dedupes them; the plugin tends to win routing despite `supabase-forgeminds` being authenticated. **Workaround: use the Supabase SQL editor for all DB operations.** Manual paste of the 3 already-applied migrations succeeded fine. Cleanup deferred — disable the plugin Supabase MCP via `/plugins → Installed → supabase → Space` whenever convenient. Not blocking.

2. **`PRODUCTION` label on the dashboard.** ForgeMinds project shows "PRODUCTION" badge — that's Supabase's default for non-branch projects, NOT actual prod. ForgeMinds has zero public traffic. Factory CLAUDE.md §5 production-data-protection rules apply once real users land here, not before.

3. **Plugin Supabase OAuth got rotated** during the session. The plugin lost old-org access mid-session, then was re-authenticated covering both orgs. **The current Claude session's cached token is stale.** Restart Claude Code at the start of the next session to pick up the fresh token. After restart, `mcp__<id>__list_projects` should show 4 projects (HuntHive, family-ceo, BookDrop, ForgeMinds).

4. **Github plugin got accidentally uninstalled** during a `/plugins` Space-press fumble. Re-install via `/plugins → Discover → search "github" → Enter`. Not blocking, do whenever.

5. **Note on `.env.local`**: the `SUPABASE_ANON_KEY` (legacy JWT format) and `SUPABASE_PUBLISHABLE_KEY` (new sb_publishable_* format) both work today. Supabase will sunset the JWT format eventually. No action required.

6. **Architect+ tier idea is captured but not built.** `VECTORS.md` documents the BYOS/Resold/Off model for licensed-data integration across all 14 vectors. Phase 7+ work. **The non-negotiable rule: ForgeMinds NEVER pre-pays for any provider before users commit money.** Resold opens per-provider only after ≥10 waitlisted Architect+ users + ≥30% projected gross margin.

---

## Kickoff prompt for the next Claude Code session

Copy-paste this verbatim into PowerShell Claude as the first message of the next session.

```
Read C:\Users\vtbsj\victor-ai-factory\projects\forgeminds\HANDOFF_2026-05-06.md before doing anything.

Then read these in this order:
1. AI_FIRST_AUDIT.md (the 5-question audit + Trust Ladder mapping)
2. DATA_FLYWHEEL.md (the contribution-types schema + cold-start copy)
3. VECTORS.md (14 vectors + ~140 subcategories + Architect+ tier)
4. CURRENT_SPRINT.md (truthful state)
5. .claude/rules/ai-first-principles.md and .claude/rules/data-flywheel.md (universal rules)

Then check git log:
  git -C . log --oneline -20

Confirm you've read the handoff by listing back to me, in 5-7 bullets:
  - Current Phase state for 0/1/1.5/2-prep
  - What's truly left for each phase
  - Known gotchas (MCP routing, PRODUCTION label, etc.)
  - The next 2 concrete actions you'd take if I said "go"

DO NOT:
- Re-derive the 14 vectors or AI-first principles — already locked in committed files
- Re-prompt me for API keys (they're already in .env.local: ANTHROPIC, OPENAI, PERPLEXITY, XAI, GOOGLE)
- Re-explain the BookDrop pivot — out of scope per my prior instruction
- Try to apply Phase 1.5 migrations again — already applied 2026-05-05 (verified)
- Touch the plugin:supabase:supabase MCP setup — workaround via SQL editor is fine for now

After confirming the read, propose ONE of these next moves:
  A. Phase 1.0 manual close walk-through (~15-20 min, mostly your hands)
  B. Phase 1.5-E catalog seeding — dispatch source-catalog-curator subagent for medicine/oncology as a test batch (~10-15 min subagent, ~$0.50-1.00 cost)
  C. Phase 1.5 cost-audit logging review — paste 3 simulated onboarding chat call logs and confirm cost-cap is honored

I'll pick. Then start.
```

---

## Working assumptions for next session

- API keys are in `.env.local`. Don't re-prompt.
- Supabase MCP is messy; use SQL editor for any new applies.
- The plan file at `~\.claude\plans\sparkling-waddling-pinwheel.md` is canonical for roadmap.
- All Phase 0 + Phase 1 + Phase 1.5 schema is applied to dev; only Phase 2 prep migration is file-only.
- Catalog is empty (0 rows). First curator subagent dispatch is the unlock for everything else in Phase 1.5.
- Factory rules are auto-loaded; AI-first + data-flywheel principles govern all design decisions. Re-read VECTORS.md before any vector-related architectural choice.

---

*Authored 2026-05-06 to bridge a long session. Delete or archive after Phase 1.5 closes.*
